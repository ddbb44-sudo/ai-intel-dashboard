# حالة سحب الثلاثين يومًا — 20 أغسطس 2026

## نتيجة السحب
| المؤشر | القيمة |
|---|---|
| النافذة | 21 يوليو ← 20 أغسطس 2026 |
| الحسابات | 69 (استُبعد @xai لأنه لا يُرجع منشورات) |
| **إجمالي المنشورات المسحوبة** | **2,367** |
| عدد الطلبات | 208 |
| **أخطاء / حالات حظر (429)** | **صفر** |
| مدة السحب | ~75 دقيقة موزعة على 6 نوافذ معدّل |

## كيف تُجنِّب الحظر
- الحد المقاس: **50 طلبًا لكل 15 دقيقة** على `UserTweets`.
- تباعد **3–4.2 ثانية** بين كل طلبين (بعشوائية بسيطة).
- **توقّف استباقي** عند بقاء 4 طلبات في النافذة — لا يُستهلك الحد كاملًا أبدًا.
- قراءة ترويسة `x-rate-limit-reset` والانتظار حتى إعادة الضبط فعليًا لا تخمينًا.
- النتيجة: لم يُسجَّل رد 429 واحد طوال العملية.

## خطأ اكتُشف وصُحِّح أثناء العمل
منطق التصفّح كان يقرأ مؤشر الصفحة التالية بنمط `"value" ثم "cursorType"`، بينما ترتيب المفاتيح في استجابة X معكوس. النتيجة أن السحب كان يتوقف عند أول 20 منشورًا لكل حساب. صُحِّح، وأُعيد **56 حسابًا** إلى الطابور لاستكمال النافذة، فارتفع الحصاد من 974 إلى 2,367 منشورًا.

## التصفية قبل التحليل — بالأرقام (لا تقليص صامت)
| المرحلة | العدد |
|---|---|
| إجمالي مسحوب | 2,367 |
| − إعادات نشر بلا إضافة | 585 |
| − ردود على حسابات أخرى | 9 |
| = مرشّحات أولية | **1,773** |
| − استبعاد آلي بعتبة إشارة منخفضة (نص قصير بلا رابط ولا اقتباس وتفاعل دون وسيط الحساب) | **478** |
| = **يُقرأ ويُحلَّل يدويًا** | **1,295** |

> الـ478 المستبعدة آليًا لم تُقرأ. هذا سقف تغطية معلن لا صامت: إن أردت خفض العتبة أُعيد إدخالها.

## نسخة احتياطية
حُفظت البيانات الخام كاملة (2,367 منشورًا · 1.4 ميغابايت) في مجلد التنزيلات على جهازك باسم:
`ai-intel-raw-30d.json`

## حسابات بلا أي منشور في النافذة
`almarzouq783` · `Al_3sli` · `Dr_Hmood` · `muath2` · `Saud_Omar` · `Copilot`
(خاملة فعليًا خلال الثلاثين يومًا — وليست خطأ في السحب)

## تغطية ناقصة معلنة
سبعة حسابات بلغت السقف الأمني (150 منشورًا) أو توقف مؤشر التصفّح عندها قبل بلوغ 21 يوليو، أبرزها `SultanAlFardan` (157 منشورًا حتى 7 أغسطس) و`rileybrown` (154 حتى 26 يوليو). تغطيتها تشمل الأسبوعين الأخيرين كاملين لا الثلاثين يومًا.

## المرحلة التالية
قراءة الـ1,295 منشورًا وتحليلها وتصنيفها، بعمق كامل للمهم ومختصر للمفيد، مع إسقاط منخفض القيمة نهائيًا.
البطاقات الحالية (91) تُدمج بأرقامها التسلسلية كما هي — لا يتغير رقم بطاقة موجودة.
