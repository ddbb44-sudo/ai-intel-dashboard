# مركز المعرفة — الذكاء الاصطناعي

لوحة عربية يملكها عزيز العليان، تجمع محتوى الذكاء الاصطناعي من X ويوتيوب
والمقالات، وتصنّفه وتشرحه بالعربية. تُنشر من GitHub Pages وتُحدَّث تلقائيًا
كل صباح.

**اقرأ هذا الملف كاملًا قبل أول تعديل.** كل قاعدة فيه وُلدت من عطل وقع فعلًا،
وتاريخه مذكور بجانبها. القاعدة التي لا تعرف سببها ستنقضها بحسن نية.

---

## القاعدة الأولى: لا تقل «تم» بلا دليل

في ٢–٣ سبتمبر ٢٠٢٦ اكتشف عزيز أن تعديلات كثيرة أُعلن نجاحها ولم تكن تعمل:
إضافة المقالة، إضافة الحساب، مزامنة المحفوظات. السبب في كل مرة عطل **صامت**:
الكود ينهار قبل أن يصل إلى السطر الذي يُبلغ المستخدم.

لذلك:

1. **نفّذ قبل أن تُخبر.** شغّل الأمر، افتح الصفحة المنشورة، اقرأ القيمة.
2. **كل ادعاء نجاح يُرفق بسطر دليل** — الأمر الذي نُفّذ أو القيمة التي قُرئت.
3. **ما لم يُختبر يُكتب صراحة:** «غير مُختبَر — السبب كذا».
4. **الفحص قبل الدفع:** `python .github/scripts/test_dedupe.py` و
   `test_jsontools.py` و `node sync/tests/test_state.mjs` و `lint_design.py`.
   وكلها تعمل تلقائيًا في `check.yml` عند كل دفعة.

عزيز يفضّل التحليل المباشر النقدي على المجاملة، والتعديل الجراحي على إعادة
الكتابة، والعربية في كل شيء.

---

## البنية

```
index.html          الصفحة والأنماط (81KB)
app.js              اللوحة كلها: Store · Prefs · Taxonomy · FilterEngine
                    · Ranking · UI · Router  (79KB — طبقات منطقية في ملف واحد)
accounts.json       الحسابات المتابَعة — المصدر الوحيد للحقيقة
data/               البطاقات: base-*.json للتاريخ · YYYY-MM-DD.json لكل يوم
                    · manifest.json (الفهرس) · state.json (آخر ما قُرئ)
sync/               خدمة حفظ التفضيلات (Netlify Function + Blobs)
.github/scripts/    خط السحب والمعالجة
.github/workflows/  التشغيل الآلي
```

**تدفّق اليوم:** `daily.yml` (٦:٠٠ الرياض) ← `daily_pull.py` ← Apify يسحب X
← تنظيف ← Claude يصنّف بدفعات ← منع التكرار ← كتابة البطاقات ← دفع ← تقرير
في Issue.

**المداخل الأخرى:** `article.yml` (مقالة كاملة من Issue) · `inbox.yml`
(رابط من البريد) · `account.yml` (إضافة حساب) · `account-toggle.yml`
(إيقاف/تشغيل حساب) · `delete.yml` (حذف بطاقة).

---

## قواعد التعديل

### ممنوع: سكربتات الترقيع

في المستودع تسعة ملفات `patch_*.py` وتسعة workflows باسم `install-N`.
**لا تضف إليها ولا تستعملها.** كانت التفافًا حول عجز جلسات Cowork عن الكتابة
في المستودع، وهي مصدر أعطال ٣١ أغسطس: سكربت يبحث عن مرساة نصية، فإن تغيّرت
خرج **بنجاح دون أن يغيّر شيئًا** — تشغيلة خضراء وملف كما هو.

عدّل الملف مباشرة وادفع. (وهذه الملفات مرشّحة للحذف.)

### الترتيب داخل بايثون

`daily_pull.py` و `process_article.py` سكربتات تُنفَّذ من أعلى لأسفل، لا وحدات.
**عرّف الدالة قبل استعمالها.** عطل الطلب #27 كان نداءً في السطر ٢٠٩ وتعريفًا
في السطر ٣٠١ — `NameError` قبل أن يصل الكود إلى خطوة التعليق، فبدا للمستخدم
أن «الإضافة لم تتم» بلا سبب ظاهر. و`pyflakes` في `check.yml` يمسك هذا الصنف،
وقد كشف عطلًا ثانيًا نائمًا في اليوم نفسه (`DATA` غير معرّف في `add_account.py`).

### لا فشل صامت

كل سكربت يعالج Issue فيه `sys.excepthook` يعلّق على الطلب بنصّ العطل ويُبقيه
مفتوحًا. **حافظ عليه.** لا عملية تنتهي بلا أثر يراه صاحبها.

### ما لم يُصنَّف لا يُوسم مقروءًا

دفعة التصنيف كانت تفشل فتُوسم منشوراتها العشرون «مقروءة» فلا تعود أبدًا
(≈٤٠ منشورًا في أربعة أيام). الآن: `ids_today` تشمل ما صُنِّف فقط، والباقي
يُقرأ غدًا. و`salvage()` تنقذ الكائنات المكتملة من أي JSON مقصوص.
وسقف مخرَج التصنيف ٢٤٠٠٠ توكن — ٨٠٠٠ كانت تُقصّ في منتصف JSON.

---

## منع التكرار (`dedupe.py`)

خبر Gemini 3.8 ظهر في سبع بطاقات لأن `cluster_id` كان نصًّا حرًّا يخترعه
النموذج (`gemini_38_` · `gemini38_l` · `gem38flash`…). **المفتاح يُحسب ولا
يُخترع:** توقيع موزون بـIDF، لأن «openai» و«claude» تتكرر في ٥–٨٪ من العناوين
فتبتلع الإشارة.

أربعة حرّاس: اختلاف رقم الإصدار · اختلاف نوع المحتوى · حصاد الأسبوع مقابل
الخبر المفرد · نافذة ثلاثة أيام.

ثلاث درجات يقين: `≥0.45` يُدمج بلا سؤال · `0.145–0.45` يُسأل النموذج سؤالًا
واحدًا لكل التشغيلة · دونها لا يُمَس. **وعند فشل النداء لا يُدمج** — التكرار
يُرى فيُحذف، أما الخبر المبتلَع فلا يعرف صاحبه أنه فُقد.

الفائز داخل العنقود **أغناها معلومةً** لا أعلاها تفاعلًا (`info_score`).

العتبات معايَرة على ٩٨٩ بطاقة حقيقية — **لا تغيّرها بلا إعادة معايرة.**

---

## أولويات عزيز

سلّم اهتمامه، من الأعلى:

1. **Skills**
2. **أدوات · MCP · Agents · إضافات**
3. **نسخ النماذج الجديدة**
4. **شروح عملية «كيف تفعل»**
5. **تجارب موثّقة بنتائج**
6. **تسعير وحدود الاستخدام** — يمسّ فاتورته
7. **أبحاث وقياسات**

**مرفوض تمامًا:** استحواذ · تمويل · تقييم شركات · أسهم · اكتتابات · تعيينات
· دعاوى · صراعات شركات. (ويبقى: التسعير · الإيقاف والتعطّل · أمن ما يستعمله.)

### الدرس الأهم في الفلترة (٣ سبتمبر)

التصنيف بمحور **شكل المحتوى** وحده يخطئ. «دراسة: ٩١٫٨٪ من ملفات Skills فيها
عيوب» وُسمت «بحث وقياس» فسقطت إلى أسفل السلّم — وهي عن السكيلز، أولويته الأولى.

**المحور الحاكم هو الموضوع لا الشكل:**

```
الموضوع (٧٠٪): سكيل · أداة يستعملها · نموذج · عالم AI عام
الشكل  (٣٠٪): شرح · إصدار · تجربة · دراسة · خبر
```

و٣٠٪ من البطاقات لا تقع في أي درجة من السلّم اليوم — أكثرها «ميزة جديدة في
أداة يستعملها»، وهي درجة ناقصة يجب أن تُضاف.

**قاعدة عامة تعلّمناها:** حين يخالف حكمُ عزيز مقياسًا بنيته، **المقياس هو
الخطأ**. قِس ما يهمّه لا ما يسهل قياسه: الغزارة والأسبقية تقيسان من يُكثر
ومن يسبق، لا من ينفع.

---

## الأدوات التي يستعملها عزيز

**أكّدها عزيز في ٣ سبتمبر ٢٠٢٦.** هذه القائمة ليست توثيقًا — هي **مدخل**
درجة «أداة يستعملها» في محور الموضوع. ما ليس فيها يهبط إلى «عالم AI عام».
فأيّ أداة تدخل عمله تُضاف هنا فورًا، وإلا فقدت أخبارُها ترتيبها.

**الثلاثي الأول — أكثر ما يعمل عليه:**

| | بطاقات في ٩٨٩ |
|---|---|
| ChatGPT · OpenAI · Codex · Sora | ٢٠٢ |
| Claude · Claude Code · Cowork | ١٦١ |
| Gemini · Google AI Studio · NotebookLM | ٧٥ |

*(كان أولها ساقطًا من هذه القائمة حتى ٣ سبتمبر — ولو نُفِّذ المحور قبل
تصحيحها لهبطت ٢٠٢ بطاقة من أكثر أدواته استعمالًا إلى «عالم AI عام».
هذا سبب التحذير أعلاه.)*

**البقية:** GitHub · Cursor · Vercel · Ollama · Netlify · Apify · Chrome ·
Google Docs و Drive و Gmail · Trello · WordPress · Chatbase ·
المكتبة الشاملة (MCP)

**ملاحظة على التغطية:** Apify و Trello و WordPress و Chatbase صفر بطاقة في
٨٧ يومًا. هذا لا يعني إخراجها — أداةٌ يستعملها تبقى كذلك وإن سكت عنها
المتابَعون؛ ويعني أن لا مصدر في القائمة يغطّيها إن صدر عنها ما يهمّه.

---

## البنية التحتية

| | |
|---|---|
| اللوحة | GitHub Pages — `ddbb44-sudo.github.io/ai-intel-dashboard` |
| حفظ التفضيلات | Netlify — `ai-intel-sync.netlify.app/prefs` |
| السحب | Apify (اشتراك مدفوع) |
| التصنيف | Claude API — ‎$0.45–0.65 يوميًا |

**Netlify يُحسب بالنقاط: كل نشر إنتاجي ١٥ نقطة من ٣٠٠ مجانية شهريًا.**
لذلك `netlify.toml` فيه `ignore` يمنع البناء إلا إذا تغيّر `sync/` أو الملف
نفسه — بدونه كانت كوميتات البيانات اليومية تستنفد النقاط في أسبوع فتتوقف
الخدمة. **لا تحذف هذا السطر.**

`sync/` خدمة صغيرة تستقبل **عمليات** (أضف/احذف حفظًا) لا حالة كاملة، فلا
يدهس جهازٌ عملَ جهاز. وتحتفظ بعشرين نسخة سابقة.

---

## ملفات لا تلمسها بلا سبب

- `data/state.json` — `recent_ids` تمنع إعادة قراءة ما قُرئ. العبث بها يعيد
  آلاف المنشورات أو يفقدها.
- `data/manifest.json` — فهرس الملفات. أي ملف بيانات جديد يجب أن يُسجَّل فيه.
- `accounts.json` — المصدر الوحيد لقائمة المتابَعة. الإيقاف بـ`active: false`
  لا بالحذف، كي لا يُفقد التاريخ.

---

## أُنجز في ٣ سبتمبر ٢٠٢٦

- حُذف ١٩ ملفًا ميتًا (`patch_*.py` · `install-*.yml` · `prefs.json`)
- أُوقفت ٦ حسابات صفر-بطاقة، وصُحّح `@xai` إلى `@SpaceXAI` — المفعّل ٦٥/٧١
- ضُغطت الفلاتر من ٣٦٨ قيمة إلى ١٠٠: الكيانات بعتبة ٣ ذكرات (٣٢٢ → ٥٩
  في الشريط والباقي خلف زر)، وطبيعة التغيير دُمجت ١٩ → ١٠ مجموعات
  (`CHG_GROUPS` في `app.js` · `CHANGE_GROUPS` في `taxonomy.py` — نسختان
  يجب أن تبقيا متطابقتين)
- أُكّدت قائمة الأدوات وأُضيف إليها ChatGPT · OpenAI · Codex

## مفتوح ولم يُنفَّذ بعد

**الجذر — كل ما تحته عرضٌ له:** لا حقل للموضوع في البطاقة. النموذج يصنّف
بالشكل والمجال، فيقدّر الأهمية من «قيمة الخبر للعالم» لا «قيمته لعزيز».

- إضافة `audience_topic` إلى `taxonomy.py` و `daily_pull.py` — ست درجات:
  سكيل · أداة يستعملها · بنية الوكلاء · نموذج · عالم AI عام · خارج الاهتمام
- إعادة وسم ٩٨٩ بطاقة بالمحور الجديد عبر `retag.yml`، وفي التشغيلة نفسها:
  إعادة `dedupe.py` على التاريخ، ومسح `cluster_id` المخترَع الذي يُخزَّن بلا استعمال
- إعادة وزن `Ranking.score` بعد ذلك — لا قبله
- بقية تنظيف الفلاتر: جدول مرادفات (`GoogleDeepMind` = `Google DeepMind`،
  و`AI Studio` = `GoogleAIStudio` = `Google AI Studio`) · شجرة الشركة الأم
  (مطلوبة في `00-التعليمات.md` وغير منفَّذة) · انقسام «برمجة وهندسة» (٣٥٢ بطاقة)
- `tool_types` فارغ في ٦٠٪ من البطاقات — اختبِر إلزام النموذج بملئه قبل
  بناء محور جديد؛ قد يغني عنه
- تغطية MCP شحيحة: ١٥ بطاقة في ٨٧ يومًا. مشكلة مصادر لا ترتيب
- Gemini/AI Studio أضعف تغطيةً بمرتين في الشروح — الحسابات الرسمية تُعلن
  ولا تشرح؛ يلزم أفراد يشرحون
- حارس خامس في `dedupe.py`: اختلاف الشركة المصدِرة يمنع الدمج (رُصد عنقود
  ضمّ خبر Google إلى خبر Meta)
- `freshness` مجمّدة: ٤١٥ بطاقة على ١٠٠٫٠ بالضبط — يشبه قيمة افتراضية عند
  فشل الحساب أكثر مما يشبه قيمة قديمة. افحص الكود قبل معالجتها كمسألة تصميم
- `importance_score` قيمتان فقط (٨٨ / ٦٢) — يُحلّ تلقائيًا بالمحور
- لا مقياس دوري لصحة النظام. مقترح `health.py` أسبوعيًا: القيم المميزة
  للأهمية · نسبة `tool_types` الفارغة · أزواج فوق العتبة غير المدموجة ·
  حسابات صفر-بطاقة
