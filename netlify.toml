# Netlify يخدم المستودع كما هو، ودوال الحفظ من sync/netlify/functions.
# اللوحة تبقى على GitHub Pages؛ هذا المشروع لخدمة /prefs فقط.
[build]
  publish = "."
  command = "echo no build"

[functions]
  directory = "sync/netlify/functions"
