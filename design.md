# design.md — قواعد لوحة «مركز المعرفة»

مرجع من يعمل على هذه اللوحة: عزيز، أو أي وكيل أو مبرمج بعده.
**اقرأه كاملًا قبل أي تعديل.** ليس توثيقًا للماضي بل شرطًا للتغيير.

## كيف يُقرأ هذا الملف

كل قاعدة تحمل **أصلها**: تاريخها والعطل الذي سبّبها. هذا مقصود.
القاعدة بلا سبب موثّق تتحوّل بعد أشهر إلى عبء لا يجرؤ أحد على حذفه لأنه
لا يعرف لماذا وُضعت. ومع الأصل يمكن سؤال: هل ما زال العطل قائمًا؟ إن لا،
فالقاعدة تُشطب.

**ولا يُكتب هنا ما يمكن لفحص آلي أن يمسكه.** المخالفات الميكانيكية مكانها
`.github/scripts/lint_design.py`. القاعدة النثرية التي لا يمكن التحقق من
تطبيقها تُهمَل بصمت — وهذا ما يحوّل الملف إلى ورق حائط.

| الرمز | المعنى |
|---|---|
| **م** | مبدأ — لا يُنقض. التعارض معه إنذار خطر |
| **ق** | قاعدة — تُراجع وتُنقض بقرار صريح |
| **~~مشطوب~~** | نُقضت عمدًا، وتُترك ليُعرف أنها ليست نسيانًا |

---

## أولويات عند التعارض

حين تتضارب قاعدتان، الأعلى في هذه القائمة يفوز. لا اجتهاد.

1. **نصّ عزيز لا يُمسّ** — ما يلصقه بيده لا يُعاد صياغته ولا يُضاف إليه
2. **لا تخمين ولا اختراع** — لا رقم ولا اسم ولا رابط غير موجود في المصدر
3. **الفشل الصامت ممنوع** — العطل يُعلَن أو لا يُعدّ مُصلَحًا
4. **الاكتمال قبل الشكل** — فقدان محتوى أفدح من فقدان تنسيق
5. **قابلية القراءة قبل الكثافة** — على الجوال قبل الديسكتوب
6. **الجمال** — آخر الأولويات، لا أولها

---

## م — مبادئ

**م1 · نصّ عزيز لا يُمسّ.**
النص الملصوق يُرتَّب ولا يُعاد صياغته ولا يُلخَّص ولا يُترجَم، ولا تُضاف إليه
ملاحظة في أوله ولا آخره. النموذج يفهرس فقط: عنوان ووسوم.
*الأصل: 31 أغسطس 2026 — بطاقة `c870`. مرّ نصّ المستخدم على النموذج فأعاد صياغته
وأسقط كلمات («الخاصة بك» ← «الخاصة») وسطّح خمسة جداول.*

**م2 · لا تخمين.**
كل ما يُكتب مستخرج من المصدر. الوسم الذي لا يمكن الدفاع عنه من النص لا يوضع،
ومخرَج النموذج يمرّ على قائمة بيضاء ويُحذف منه كل وسم مخترَع.
*الأصل: §33 — قاعدة المشروع منذ نشأته.*

**م3 · الفشل الصامت ممنوع.**
كل عطل يُعلَن: خطأ ظاهر يوقف التشغيلة، أو تعليق على الطلب، أو سطر صريح في
البطاقة. ولا حلقة إعادة محاولة تبتلع فشلًا.
*الأصل: 31 أغسطس 2026 — `install-account-flow` ظهر أخضر ناجحًا وهو لم يكتب شيئًا،
لأن حلقة `for i in 1 2 3` ابتلعت رفض GitHub. ضاع نصف يوم على عطل مُعلَن أصلًا في السجل.*

**م4 · ما يضيفه عزيز بيده لا يُرفض أبدًا.**
الحَكَم الذي يرفض ~60٪ من تغريدات الحسابات لا سلطان له على ما يختاره بنفسه.
أدنى تصنيف له «مفيد».
*الأصل: قاعدة المشروع في `INBOX.md`.*

---

## ق — قواعد المحتوى

**ق1 · الاكتمال قبل الشكل.**
عند الاختيار بين نسختين من محتوى، تُبقى الأكمل ولو كانت أقل تنسيقًا.
*الأصل: 1 سبتمبر 2026 — لصق Google Docs يحمل نسختين متكاملتين. أبقيتُ الأغنى
جداولَ فخسرتُ أربعة أقسام من خمسة.*

**ق2 · لا تُختلق بنية غير موجودة.**
العنوان الفرعي عنوانٌ فقط إن كان معلَّمًا في المصدر (`<h1-6>` أو `##`).
السطر القصير سطرٌ قصير، لا عنوان.
*الأصل: 31 أغسطس 2026 — خمّنتُ أن «كل سطر قصير بلا نقطة = عنوان»، فأنتجتُ
99 عنوانًا من 126 فقرة، لأن خلايا الجداول أسطر قصيرة أيضًا.*

**ق3 · الروابط لا تُمحى.**
تنظيف HTML يحفظ `<a href>` كـ`[نص](رابط)`. نزع الوسوم بلا استثناء إتلافٌ
لا تنظيف.
*الأصل: 31 أغسطس 2026 — `clean_html` كانت تمحو كل روابط المقالة.*

**ق4 · رابط في المتن مرجعٌ لا مصدر.**
لا يُنسب رابط ورد داخل نص ملصوق كمصدر للبطاقة.
*الأصل: 31 أغسطس 2026 — `c870` نُسبت إلى رابط بحث يوتيوب ورد في متنها.*

**ق5 · البتر يُعلَن.**
إن بلغت المقالة سقف المعالجة، يُكتب في آخرها صراحةً أنها قد تكون مبتورة.
*الأصل: مشتقة من م3.*

---

## ق — قواعد الواجهة

**ق6 · الرابط فعلٌ لا بيان.**
لا يُدفن رابط في خانة جدول. كل رابط يظهر زرًّا مستقلًا واضحًا.
*الأصل: 1 سبتمبر 2026 — رصده عزيز. الرابط كان آخر عمود، وفي RTL آخر عمود هو
الأقصى يسارًا: أول ما يختفي عند التمرير. قياس: عند عرض 760px خرج من الشاشة تمامًا.*

**ق7 · الجدول العريض يصير بطاقات على الجوال.**
أكثر من أربعة أعمدة تحت 760px تُعرض بطاقاتِ مفتاح/قيمة، لا تمريرًا أفقيًا.
*الأصل: 31 أغسطس 2026 — جدول بعشرة أعمدة على شاشة 390px.*

**ق8 · الأقران المتكافئون لا يختلفون.**
أزرار الشريط العلوي أقران: مقاس واحد وحشو واحد. أي اختلاف يُرى بالعين فورًا.
*الأصل: 1 سبتمبر 2026 — رصده عزيز بالعين ولم يرصده قياسي، لأن الحاوية بقيت
36×36 بينما الحشو الداخلي سحق الأيقونة. الفحص الآلي رقم 2 يمسكها الآن.*

**ق9 · سطر القراءة لا يتجاوز 68 حرفًا.**
السطر الطويل يُتعب العين العربية.

**ق10 · لا يدخل HTML من نصّ المستخدم.**
يُهرَّب النص أولًا ثم تُضاف وسومنا. لا استثناء.

**ق11 · نسخة `app.js` تُربط ببصمة محتواه.**
لا بتاريخ البيانات ولا برقم يدوي.
*الأصل: 31 أغسطس 2026 — زر المقالة ظهر ولم يعمل. `index.html` جديد و`app.js`
قديم من الذاكرة المؤقتة، لأن الرابط كان `app.js?v=<تاريخ البيانات>` ولم تتغيّر
البيانات. كل تعديل سابق على اللوحة عانى منه دون أن ندري.*

---

## أنماط ممنوعة

أشكال وقعت فعلًا ولا نريد رؤيتها ثانية:

- **حارس يفحص علامة قديمة.** مُرقِّع يتخطى التحديث لأن الميزة «موجودة».
  *(31 أغسطس — `patch_tables` فحص `artTable` فتخطّى نسخته الجديدة، فضاعت الروابط بين `install-5` و`install-6`.)*
- **اسم فئة CSS يخدم غرضين.** *(1 سبتمبر — `.artbtn` للشريط وللمقالة معًا.)*
- **زر بلا نظير.** إضافة فعل بلا فعله المقابل.
  *(اللوحة ملكت ثلاث طرق للإضافة وصفرًا للحذف حتى 1 سبتمبر.)*
- **رابط بلا حارس.** `href=""` لبطاقة بلا مصدر يعيد تحميل الصفحة.
- **رمز X لكل المصادر.** أيقونة مصدر ثابتة مهما كان نوعه.
- **جدار `<br>`.** نص طويل في `<p>` واحدة بلا فقرات.
  *(31 أغسطس — 444 بطاقة من 870 كانت تُعرض كتلةً واحدة.)*
- **إعادة استخدام رقم بطاقة محذوفة.** الرقم يُحرق ولا يُعاد.

---

## عند التعارض — الإجراء

1. **قِف.** لا تنفّذ التعليمة الجديدة قبل الفصل.
2. **سَمِّ المتعارضين.** «تعليمتك تناقض ق1 من 1 سبتمبر، وكان أصلها كذا.»
3. **اسأل عزيز.** أهو نقضٌ مقصود أم استثناء لحالة واحدة؟
4. **دوّن الحكم.** النقض يشطب القاعدة **ولا يحذفها**، مع تاريخه وسببه.
   الاستثناء يُكتب داخل القاعدة نفسها.

وليس كل تعارض خطأ: قول عزيز «المقالة كاملة» ثم «لا تعدّل النص إطلاقًا» لم
يكن تناقضًا بل تصحيحًا. الآلية تفرز، والقرار له.

---

## طبقات الإصلاح

قبل كتابة أي قاعدة هنا، اسأل: أهذه طبقتها الصحيحة؟

| نوع الخطأ | مكانه |
|---|---|
| حكم وذوق لا يُقاس | **هذا الملف** |
| قيمة متكررة | متغيّر CSS في `index.html` |
| مخالفة ميكانيكية | `lint_design.py` — **يُفضَّل دائمًا** |

القاعدة القابلة للبرمجة تُبرمَج. النثر آخر ملاذ لا أوله.
